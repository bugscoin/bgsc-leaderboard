import React from 'react';

const RegisterWalletButton = () => {
  return (
    <div style={{textAlign: 'center', marginBottom: '32px'}}>
      <button style={{padding: '16px 48px', fontSize: '18px', fontWeight: '600', borderRadius: '12px', background: 'white', color: '#667eea', border: 'none', cursor: 'pointer'}}>
        Register Wallet
      </button>
    </div>
  );
};

export default RegisterWalletButton;
